# commitish: development — the workflow triggers on push to development (where all PRs
# merge), so Release Drafter scans development to find PR labels and resolve the version.
# The draft release targets development; before publishing, manually change the "Target"
# dropdown to main in the GitHub release UI so the tag lands on main.
commitish: development
name-template: "$RESOLVED_VERSION"
tag-template: "v$RESOLVED_VERSION"
exclude-labels:
    - "skip-changelog"

categories:
    - title: "🚀 Features"
      labels:
          - feature
          - enhancement
    - title: "🐛 Bug Fixes"
      labels:
          - fix
          - bugfix
          - bug
    - title: "🧰 Maintenance"
      label: chore
    - title: ":package: Dependencies"
      labels:
          - dependencies
          - build
          - dependabot
          - DEPENDABOT
version-resolver:
    major:
        labels:
            - major
    minor:
        labels:
            - minor
            - feature
            - enhancement
    patch:
        labels:
            - patch
            - fix
            - bugfix
            - bug
            - chore
            - dependencies
            - build
    default: patch
autolabeler:
    - label: chore
      files:
          - "*.md"
      branch:
          - '/docs{0,1}\/.+/'
    - label: bug
      branch:
          - /fix\/.+/
      title:
          - /fix/i
    - label: enhancement
      branch:
          - /feature\/.+/
template: |
    ## Changes

    $CHANGES

name: Publish spectrum_fundamentals to PyPI

on:
    release:
        types: [published]

# Never cancel a release publish once started.
concurrency:
    group: ${{ github.workflow }}-${{ github.ref }}
    cancel-in-progress: false

jobs:
    # Re-run CI at the exact commit being released as a hard gate before publishing.
    ci:
        name: CI gate
        uses: ./.github/workflows/ci.yml

    release:
        name: Release
        runs-on: ubuntu-latest
        needs: ci

        # Required for PyPI Trusted Publishing (OIDC) — no PYPI_TOKEN secret needed.
        permissions:
            id-token: write
        steps:
            - name: Check out the repository
              uses: actions/checkout@v6

            - name: Set up Python
              uses: actions/setup-python@v6
              with:
                  python-version: "3.13"
                  cache: pip

            - name: Install Poetry
              run: |
                  pip install poetry
                  poetry --version

            - name: Build package
              run: poetry build --ansi

            - name: Publish package on PyPI
              uses: pypa/gh-action-pypi-publish@v1.13.0
